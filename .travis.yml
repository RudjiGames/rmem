language: cpp
matrix:
  include:
    - compiler: gcc
      os: linux
    - compiler: clang
      os: osx

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - clang

before_script:
  - git clone --depth 1 https://github.com/milostosic/build ../build

script:

branches:
  only:
    - master

notifications:
  email: false

osx_image: xcode7.3

